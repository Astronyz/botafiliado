services:
  botafiliado:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "${WEB_PORT:-3000}:${WEB_PORT:-3000}"
    environment:
      DATABASE_PATH: /app/data/botafiliado.sqlite
    volumes:
      - ./data:/app/data
